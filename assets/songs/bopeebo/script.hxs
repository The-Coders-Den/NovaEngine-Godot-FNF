function onBeatHit(curBeat) { // lmao i forgor to rename
    if (curBeat % 8 == 7) {
        if(boyfriend != null) {
            boyfriend.animTimer = 0.6;
            boyfriend.playAnim('hey', true);
            boyfriend.specialAnim = true;
        }
    }
}